apiVersion: v1
kind: Service
metadata:
  name: transfers-svc
  namespace: veerabank
spec:
  type: ClusterIP
  selector:
    app: transfers
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
