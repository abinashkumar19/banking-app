apiVersion: v1
kind: Service
metadata:
  name: statements-svc
  namespace: veerabank
spec:
  type: ClusterIP
  selector:
    app: statements
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
