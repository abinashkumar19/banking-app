apiVersion: v1
kind: Service
metadata:
  name: cheques-svc
  namespace: veerabank
spec:
  type: ClusterIP
  selector:
    app: cheques
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
