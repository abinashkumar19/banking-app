apiVersion: v1
kind: Service
metadata:
  name: transactions-svc
  namespace: veerabank
spec:
  type: ClusterIP
  selector:
    app: transactions
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
