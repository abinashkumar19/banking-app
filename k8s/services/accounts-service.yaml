apiVersion: v1
kind: Service
metadata:
  name: accounts-svc
  namespace: veerabank
spec:
  type: ClusterIP
  selector:
    app: accounts
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
