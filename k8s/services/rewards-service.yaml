apiVersion: v1
kind: Service
metadata:
  name: rewards-svc
  namespace: veerabank
spec:
  type: ClusterIP
  selector:
    app: rewards
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
