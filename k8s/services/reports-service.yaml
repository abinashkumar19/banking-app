apiVersion: v1
kind: Service
metadata:
  name: reports-svc
  namespace: veerabank
spec:
  type: ClusterIP
  selector:
    app: reports
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
