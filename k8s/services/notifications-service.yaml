apiVersion: v1
kind: Service
metadata:
  name: notifications-svc
  namespace: veerabank
spec:
  type: ClusterIP
  selector:
    app: notifications
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
