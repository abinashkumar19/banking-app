apiVersion: v1
kind: Service
metadata:
  name: disputes-svc
  namespace: veerabank
spec:
  type: ClusterIP
  selector:
    app: disputes
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
